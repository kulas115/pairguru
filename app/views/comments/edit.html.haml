= form_for([movie,comment]) do |f|
  .row
    .col-md-4
      .form-group
        = f.text_area :content, class: 'form-control', rows: 5, placeholder: "Comment content"
      .form-group
        = f.submit 'Submit', class: "btn btn-primary btn-block"
